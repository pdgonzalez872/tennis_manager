<ul class="round round-1">
  <li class="spacer">&nbsp;</li>

  <% draw.matches.round(round_name).order('match_number asc').each do |m| %>
    <%= render partial: "matches/match", locals: {match: m} %>
  <% end %>

</ul>
